[project]
name = "cvnewtheme-assets"
version = "0.0.0"
description = "Locked, offline font generation for cvnewtheme"
requires-python = "==3.14.6"
dependencies = [
  "fonttools[woff]==4.63.0",
]

[tool.uv]
package = false
